{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "matchFiles": ["charts/{{arg0}}/Chart.yaml", "charts/{{arg0}}/Chart.lock"],
      "groupName": "charts `{{arg0}}` dependencies",
      "groupSlug": "{{arg0}}"
    }
  ]
}
